# Исполняемый файл или иные документы

Отдельный бинарный исполняемый файл не формируется. Проект запускается из исходного кода командой:

```bash
bash run_all.sh
```

Скрипт устанавливает зависимости, запускает тесты, выполняет CLI-demo и поднимает backend/frontend.
